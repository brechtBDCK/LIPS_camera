[project]
name = "lips-camera"
version = "0.1.0"
description = "Workspace to acquire LIPS camera data via code"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.4",
    "opencv-python>=4.13.0.92",
    "openni>=2.3.0",
    "pipreqs>=0.5.0",
]
